import { PricingPage } from "@/components/PricingPage";

export const metadata = {
  title: "Pricing — MetaStrip",
  description:
    "Simple one-time pricing for batch metadata removal. No subscriptions, no account needed.",
};

export default function Page() {
  return <PricingPage />;
}
